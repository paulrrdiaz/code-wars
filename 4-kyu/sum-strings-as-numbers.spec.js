import { sumStrings } from "./sum-strings-as-numbers";

describe("Sum Strings as Numbers", () => {
  it("should pass", () => {
    // expect(sumStrings("1", "2")).toBe("3");
    // expect(sumStrings("123", "456")).toBe("579");
  });
});
