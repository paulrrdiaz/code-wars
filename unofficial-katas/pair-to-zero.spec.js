import { pairToZero } from "./pair-to-zero";

describe("Pair to zero", () => {
  it("should pass", () => {
    const array1 = [-3, -2, 0, 1, 3];
    const array2 = [-3, -2, 0, 1, 5, 13, 19];
    const array3 = [0];
    // expect(pairToZero(array1)).toBe(true);
    // expect(pairToZero(array2)).toBe(false);
    // expect(pairToZero(array3)).toBe(false);
  });
});
